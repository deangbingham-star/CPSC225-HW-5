/******************************************************************************
Dean Bingham
CSPS 225
9/25/2026
Help from mom
Fraction Calculator that totally works 100%
*******************************************************************************/
#include <iostream>
using namespace std;





/* TO DO LIST
Make fraction class +
Default constructor for fraction class
Overload constructor
Make 2 functions OUTSIDE fraction class
Do not allow user to put 0 in denominator

in Main:
    Create 2 fraction objects then
    Multiply 2 fraction objects
    Simplify fraction to mixed fraction
    display all fraction

*/


//**********************************************


class fraction
{
public:

	fraction()
	{
		cout<<"\nDebug.Created Fraction\n"<<endl;
	}


	fraction(int n, int d, int w)
	{
		numerator = n;
		denominator = d;
		wholeNum = w;
	}


	int getNumerator() {
		return numerator;
	}
	int getDenominator() {
		return denominator;
	}
	int getWholeNum() {
		return wholeNum;
	}

private:
	int numerator;
	int denominator;
	int wholeNum;


};
//***********************************************
int mixify(int numerator, int denominator, int wholeNum)  //Mix improper fraction into mixed fraction
{

	while (numerator >= denominator)
	{
		wholeNum ++;
		numerator = numerator - denominator;
	}

	//	displayNum(numerator,denominator,wholeNum);
	return 0;
}
//***********************************************



fraction displayNum(fraction) //display function as improper function
{
    numerator = fraction.numerator;
    denominator = fraction.denominator;
    wholeNum = fraction.wholeNum;
    
	cout<<"Full Number: "<<wholeNum<<" "<<numerator<<"/"<<denominator<<endl;
	return 0;
}
//*************************************************
int multiply() //take two fractions, multiply. Store results in third fraction
{
	return 0;
}

//*************************************************8
int main()
{
	std::cout<<"Program Started\n";

	/*/testing for mixed fraction math
	int numerator = 11;
	int denominator = 2;
	int wholeNum = 0;

	while (numerator >= denominator)
	{
		wholeNum ++;
		numerator = numerator - denominator;
	}

	displayNum(numerator,denominator,wholeNum);
	*/
	fraction fract1(11,2,0);


	fraction fract2(1,3,0);

	displayNum(fract1);
	
	fraction fract3; // no arguments

	return 0;
}
